/* App Styling */
body { 
    font-family: sans-serif;
    font-size: 16px;
    color: #333;
    background-color: #fafafa;
}

h1, h2, h3 {
    font-weight: 600;
    margin-top: 0;
    margin-bottom: 1rem;
}

button {
    padding: 5px 10px;
    background-color: #fafafa;
    border: none;
    cursor: pointer;
}

.container {
    max-width: 800px;
    margin: auto;
    padding
